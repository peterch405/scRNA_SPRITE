__default__:
    time: "05:00:00"
    mem: 20g
    cpus: 1
    nodes: 1
    output: "{out_dir}workup/logs/cluster/{rule}.{wildcards.sample}.out"
    error: "{out_dir}workup/logs/cluster/{rule}.{wildcards.sample}.err"
log_config:
    time: "05:00:00"
    mem: 20g
    cpus: 1
    nodes: 1
    output: "{out_dir}workup/logs/cluster/{rule}.out"
    error: "{out_dir}workup/logs/cluster/{rule}.err"
cat_ligation_efficiency:
    time: "05:00:00"
    mem: 20g
    cpus: 1
    nodes: 1
    output: "{out_dir}workup/logs/cluster/{rule}.out"
    error: "{out_dir}workup/logs/cluster/{rule}.err"
bowtie2_align:
    time: "12:00:00"
    mem: 50g
    cpus: 10
    nodes: 1
    output: "{out_dir}workup/logs/cluster/{rule}.{wildcards.sample}.out"
    error: "{out_dir}workup/logs/cluster/{rule}.{wildcards.sample}.err"
multiqc:
    time: "05:00:00"
    mem: 20g
    cpus: 1
    nodes: 1
    output: "{out_dir}workup/logs/cluster/{rule}.out"
    error: "{out_dir}workup/logs/cluster/{rule}.err"
plot_cluster_size:
    time: "05:00:00"
    mem: 50g
    cpus: 1
    nodes: 1
    output: "{out_dir}workup/logs/cluster/{rule}.out"
    error: "{out_dir}workup/logs/cluster/{rule}.err"
star_align:
    time: "24:00:00"
    mem: 50g
    cpus: 10
    nodes: 1
    output: "{out_dir}workup/logs/cluster/{rule}.{wildcards.sample}.out"
    error: "{out_dir}workup/logs/cluster/{rule}.{wildcards.sample}.err"